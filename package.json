{
  "name": "react-interview-bot",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "build": "rimraf dist && tsc -p tsconfig.json",
    "build:functions": "tsc -p tsconfig.functions.json",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@netlify/functions": "^2.4.1",
    "dotenv": "^16.4.5",
    "pino": "^9.4.0",
    "pino-pretty": "^11.2.2",
    "telegraf": "^4.16.3",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.12.12",
    "rimraf": "^5.0.10",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}

