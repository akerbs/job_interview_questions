# React Interview Bot

Телеграм-бот для подготовки к собеседованиям на позицию Senior Frontend React Developer.

## Возможности

- 📝 Вопросы с 4 вариантами ответов (только один правильный)
- 🎯 Категории: React Core, React Hooks, JavaScript, TypeScript, Performance
- 📊 Статистика ответов с точностью
- ⏹️ Возможность остановить тест в любой момент
- 🔄 Сброс статистики

## Установка

1. Установите зависимости:
```bash
npm install
```

2. Создайте файл `.env` в корне проекта:
```
BOT_TOKEN=your_telegram_bot_token_here
LOG_LEVEL=info
```

3. Получите токен бота у [@BotFather](https://t.me/BotFather) в Telegram

## Запуск

### Режим разработки
```bash
npm run dev
```

### Сборка и запуск
```bash
npm run build
npm start
```

## Команды бота

- `/start` - Главное меню
- `/stop` - Остановить тест
- `/stats` - Показать статистику
- `/help` - Показать справку

## Структура проекта

```
src/
  config/          # Конфигурация (env)
  data/            # База вопросов
  handlers/        # Обработчики команд
  platforms/       # Адаптеры платформ (Telegram)
  services/        # Бизнес-логика
  types/           # TypeScript типы
  index.ts         # Точка входа
```

## Добавление вопросов

Вопросы находятся в файле `src/data/questions.ts`. Каждый вопрос имеет:
- `id` - уникальный идентификатор
- `category` - категория вопроса
- `question` - текст вопроса
- `answers` - массив из 4 вариантов ответа
- `correctAnswerIndex` - индекс правильного ответа (0-3)
- `explanation` - объяснение (опционально)

## Категории вопросов

Бот включает **более 60 вопросов** по следующим категориям:

### Frontend Core
- **HTML** - семантические элементы, структура документа, атрибуты, DOCTYPE
- **CSS** - Flexbox, Grid, позиционирование, специфичность, единицы измерения, BEM
- **JavaScript** - замыкания, прототипы, this, bind/call/apply, Promise, async/await, Event Loop, hoisting

### React & Frameworks
- **React Core** - Virtual DOM, JSX, Reconciliation, Fiber, Error Boundary, Portals
- **React Hooks** - useState, useEffect, useMemo, useCallback, useRef, useContext, useReducer и другие
- **Next.js** - SSR, SSG, ISR, Server Components

### State Management & Testing
- **State Management** - Redux, Context API, Redux Toolkit
- **Testing** - unit тесты, React Testing Library

### Browser & Web APIs
- **DOM & Events** - фазы событий, делегирование, event.target vs currentTarget, debounce/throttle
- **Browser & Web APIs** - LocalStorage, SessionStorage, CORS, Web Workers, Service Workers

### Performance & Architecture
- **Performance** - Code Splitting, Lazy Loading, виртуализация, Tree Shaking, Critical Rendering Path, reflow/repaint
- **Architecture** - Component Composition, Render Props, паттерны проектирования

## Особенности

- ✅ **Более 60 вопросов** по различным темам для Senior Frontend React Developer
- ✅ 4 варианта ответа на каждый вопрос (только один правильный)
- ✅ **Развёрнутые объяснения** правильных ответов
- ✅ **Подсветка правильного ответа** после каждого вопроса
- ✅ Статистика с точностью ответов
- ✅ Возможность остановить тест в любой момент
- ✅ Сброс статистики
- ✅ Случайный выбор вопросов без повторений

## Разработка

Проект использует:
- TypeScript для типобезопасности
- Telegraf для работы с Telegram Bot API
- Pino для логирования
- Zod для валидации конфигурации

## Развертывание

Проект можно развернуть на Netlify. Подробная инструкция в [NETLIFY_SETUP.md](./NETLIFY_SETUP.md)

### Быстрый старт для Netlify:

1. Подключите репозиторий к Netlify
2. Установите переменные окружения: `BOT_TOKEN`
3. Настройте webhook в Telegram после развертывания
4. Готово! Бот работает через Netlify Functions

